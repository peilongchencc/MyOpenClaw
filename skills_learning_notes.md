# AI Agent Skills 学习笔记

本文整理自与 AI 的对话，系统梳理了 Skills 的概念、原理、使用方式和行业趋势。

- [AI Agent Skills 学习笔记](#ai-agent-skills-学习笔记)
  - [一、Skills 是什么？](#一skills-是什么)
  - [二、Skills 的工作原理](#二skills-的工作原理)
    - [2.1 核心机制：外置知识 + 动态注入](#21-核心机制外置知识--动态注入)
    - [2.2 关键机制总结](#22-关键机制总结)
  - [三、为什么大家这么推崇 Skills？](#三为什么大家这么推崇-skills)
  - [四、"你会被蒸馏成 Skills"是什么意思？](#四你会被蒸馏成-skills是什么意思)
  - [五、Skills 概念的起源](#五skills-概念的起源)
    - [5.1 演化脉络](#51-演化脉络)
    - [5.2 各家框架的叫法对比](#52-各家框架的叫法对比)
    - [5.3 Anthropic 的角色](#53-anthropic-的角色)
  - [六、Skills vs RAG](#六skills-vs-rag)
    - [6.1 相似但不同](#61-相似但不同)
    - [6.2 维度对比](#62-维度对比)
  - [七、Skill 的匹配机制：大模型如何选择 Skills？](#七skill-的匹配机制大模型如何选择-skills)
    - [7.1 两阶段检索](#71-两阶段检索)
    - [7.2 上下文够用吗？](#72-上下文够用吗)
    - [7.3 应对上下文压力的策略](#73-应对上下文压力的策略)
  - [八、意图识别原理](#八意图识别原理)
    - [8.1 传统 NLU vs 大模型时代](#81-传统-nlu-vs-大模型时代)
    - [8.2 代码示例：模拟 Skill 匹配过程](#82-代码示例模拟-skill-匹配过程)
  - [九、如何创建和使用 Skills？](#九如何创建和使用-skills)
    - [9.1 你需要做什么 vs 系统自动做什么](#91-你需要做什么-vs-系统自动做什么)
    - [9.2 Skill 的两种来源](#92-skill-的两种来源)
    - [9.3 一个 SKILL.md 的示例](#93-一个-skillmd-的示例)
  - [十、未来趋势](#十未来趋势)

---

## 一、Skills 是什么？

Skills（技能）本质上是**结构化的"指令文档"**，通常是一个 Markdown 文件（如 `SKILL.md`），里面封装了：

- 某个专业领域的知识和最佳实践
- 一套标准的执行工作流（做什么、怎么做、顺序是什么）
- 边界条件和注意事项

**类比**：如果 AI Agent 是一个员工，Skills 就是你发给他的"标准作业程序（SOP）"。

**工作流示例**：当用户说"帮我做一个 PPT"时，Agent 会：

1. 识别到这个任务匹配 `slides/SKILL.md`
2. 读取该文件，获取"如何制作 PPT 的完整专业流程"
3. 按照 Skill 中的指令执行，而不是凭空猜测

---

## 二、Skills 的工作原理

### 2.1 核心机制：外置知识 + 动态注入

```
传统方式：知识烧死在模型权重里（静态、不可修改）
         用户问题 → 模型直接回答

Skills方式：知识存在外部文件里（动态、可维护）
         用户问题 → Agent识别意图 → 读取对应Skill文件 → 执行标准化流程
```

这和软件工程中的"策略模式"很像——行为是可插拔的，而不是硬编码的。

### 2.2 关键机制总结

| 机制 | 作用 |
|---|---|
| 意图识别 | Agent 判断当前任务需要哪个 Skill |
| 动态读取 | 运行时才加载 Skill 内容注入上下文 |
| 流程约束 | Skill 定义执行顺序，防止 Agent 乱发挥 |
| 可组合 | 复杂任务可同时加载多个 Skill |

---

## 三、为什么大家这么推崇 Skills？

**1. 解决了"模型幻觉"问题**

模型对某个专业领域（比如公司的内部流程）一无所知，但 Skill 可以把领域知识喂给它，让它按照正确流程执行。

**2. 可维护、可迭代**

传统的 Prompt Engineering 往往是一次性的，难以维护。Skill 文件像代码一样可以版本管理、持续优化。发现 Agent 做某件事不对，直接改 Skill 文件即可，不需要重新训练模型。

**3. 知识的"模块化"**

和软件工程一样，Skills 实现了知识的"单一职责原则"——每个 Skill 只负责一件事，可以复用、可以组合。

**4. 降低了专业门槛**

写好一个"代码审查 Skill"，团队里所有人、所有 Agent 都能以专家级水准来做代码审查。知识从个人转移到了组织资产。

---

## 四、"你会被蒸馏成 Skills"是什么意思？

"知识蒸馏"（Knowledge Distillation）原本是 AI 领域的术语，指把大模型的能力压缩进小模型。现在被引申为：**把人类的专业知识、经验、工作流程，提炼编码成 AI 可以直接调用的 Skill 文件**。

以算法工程师的工作为例：

```
今天：你 → 手动完成 NLP 算法设计、Python 接口开发、调试

未来：
  你的经验 → 被整理成 Skill
  skill_nlp_design.md    （你的算法选型经验）
  skill_fastapi_build.md  （你的接口规范）
  skill_debug_python.md   （你的调试方法论）
  
  → Agent 直接调用这些 Skill，完成 80% 的日常工作
```

**两层含义**：

- **乐观解读**：你的专业价值被永久保留和放大，你成为了一个"知识源"，影响力超越了个人工作时长的限制。
- **警示解读**：如果你的工作是高度流程化、可标准化的，那么一旦被"蒸馏"成 Skill，你这个"人"在这个环节就可以被替代了。

**核心竞争力方向**：创新性研究、复杂系统设计决策、跨团队沟通协调——这些是"不可蒸馏的"。

---

## 五、Skills 概念的起源

### 5.1 演化脉络

**Skills 这个概念不是某一家公司发明的**，它是逐步演化而来的：

1. **机器人学 & 经典 AI（1990s-2010s）**：Skill 最早来自机器人学和强化学习，指可复用的子策略（sub-policy）或行为原语（behavior primitive）。
2. **Microsoft Semantic Kernel（2023 年初）**：真正把 Skills 概念带入 LLM Agent 生态。核心抽象就叫 Skills，后改名为 Plugins。
3. **各家框架各说各话（2023）**：不同框架对类似概念有不同叫法。
4. **SKILL.md 模式（2024-2025）**：社区在编程助手生态中自发演化出的最佳实践，被 Cursor 等工具采纳推广。

### 5.2 各家框架的叫法对比

| 框架 | 叫法 |
|---|---|
| Microsoft Semantic Kernel | Skills → Plugins |
| LangChain | Tools / Chains |
| AutoGPT | Commands |
| OpenAI | Function Calling / Plugins |
| Anthropic | Tool Use |

### 5.3 Anthropic 的角色

Anthropic 对 Skills 领域的核心贡献不在概念本身，而在底层能力：

- **工具调用能力（Tool Use）**：让 Claude 能可靠地调用外部工具
- **长上下文窗口**：使 Agent 能读取大量 Skill 文档而不丢失信息
- **可靠的指令遵循**：使 Agent 能严格按 Skill 文件中的流程执行

> Anthropic 提供的是**让 Skills 能跑起来的底层能力**，而不是 Skills 这个上层概念本身。

---

## 六、Skills vs RAG

### 6.1 相似但不同

Skills 是 RAG 的一个**特例/子集**，底层机制都是"检索 + 注入上下文"：

```
RAG（通用）：
  用户问题 → 检索相关文档片段 → 注入上下文 → 生成回答
  特点：检索的是"信息"，用来回答问题

Skills（专用）：
  用户意图 → 匹配对应技能文档 → 注入上下文 → 按流程执行
  特点：检索的是"操作手册/SOP"，用来执行任务
```

### 6.2 维度对比

| 维度 | RAG | Skills |
|---|---|---|
| 检索对象 | 知识片段（段落、表格） | 执行流程（SOP、工作流） |
| 目的 | 回答问题 | 完成任务 |
| 注入内容的性质 | 信息/事实 | 指令/步骤 |
| 典型产出 | 一段文字回答 | 一系列工具调用和操作 |

> Skills 更像是给 Agent 发了一本"操作手册"，而不是一篇"参考文献"。

---

## 七、Skill 的匹配机制：大模型如何选择 Skills？

### 7.1 两阶段检索

大模型**不需要查看所有 Skills 的全文**，而是采用两阶段匹配：

```
第一步：System Prompt 中包含所有 Skills 的"标题+简短描述"（几十字/条）
       占用的上下文非常小
       
第二步：用户发来请求 → 大模型根据描述判断是否匹配某个 Skill

第三步：如果匹配 → 用 Read 工具读取那个 SKILL.md 的完整内容
       如果不匹配 → 直接用自己的能力回答

第四步：根据 Skill 内容执行
```

这和 RAG 中的"粗筛 + 精排"是一样的思路。

### 7.2 上下文够用吗？

| 因素 | 情况 |
|---|---|
| 目录摘要 | 几十个 Skills 的标题+描述，大概几千 token，问题不大 |
| 单个 Skill 全文 | 通常 500-3000 token，可控 |
| 同时加载多个 Skill | 2-3 个就可能占 5000-10000 token，开始需要小心了 |
| 加上用户代码、对话历史 | 上下文压力确实会增大 |

### 7.3 应对上下文压力的策略

1. **Skill 文件要写得精简**——这是设计 Skill 的核心原则
2. **按需加载，用完即弃**——不会一直把所有 Skill 驻留在上下文里
3. **长上下文模型支持**——Claude 有 200K token 的窗口
4. **层级化**——把大 Skill 拆成小 Skill

---

## 八、意图识别原理

### 8.1 传统 NLU vs 大模型时代

大模型做意图识别**不是用 if-else，而是用语义理解做"软匹配"**：

```
传统 NLU（2020年之前）：
  "帮我做个PPT" → 分词 → 意图分类器 → intent=make_ppt → 路由到PPT模块
  问题：新增一个意图就要重新训练分类器

大模型时代（现在）：
  "帮我做个PPT" → 大模型读到 Skill 目录 → 理解"这个请求和slides技能描述最匹配"
  优势：新增一个 Skill 只需要加一条描述，不需要训练任何东西
```

**大模型的"意图识别"可以扩展到无限多意图**——它不是在做分类，而是在做阅读理解。

### 8.2 代码示例：模拟 Skill 匹配过程

```python
"""
模拟大模型如何做意图识别来匹配 Skills。

核心思路：不是硬编码意图，而是把每个 Skill 的"触发条件"描述清楚，
让大模型自己做语义匹配。
"""

# ============ 第一部分：Skill 目录（注入到 System Prompt 中） ============

SKILL_REGISTRY = [
    {
        "id": "slides",
        "path": "skills/slides/SKILL.md",
        "trigger_description": "创建、编辑、渲染PPT/幻灯片/演示文稿",
    },
    {
        "id": "spreadsheet",
        "path": "skills/spreadsheets/SKILL.md",
        "trigger_description": "创建、修改、分析电子表格/Excel/CSV文件",
    },
    {
        "id": "fastapi_build",
        "path": "skills/fastapi/SKILL.md",
        "trigger_description": "构建FastAPI接口、设计API路由、处理请求响应",
    },
    {
        "id": "debug_python",
        "path": "skills/debug/SKILL.md",
        "trigger_description": "调试Python代码、排查错误、分析日志",
    },
]

# ============ 第二部分：构建 System Prompt ============

def build_system_prompt(skills: list[dict]) -> str:
    """构建包含 Skill 目录的系统提示词。"""
    skill_section = "可用技能列表：\n"
    for s in skills:
        skill_section += f"- [{s['id']}] {s['trigger_description']} → 文件：{s['path']}\n"

    return f"""你是一个 AI 编程助手。

当用户发来请求时，判断是否需要使用某个技能：
{skill_section}

规则：
1. 如果用户的请求匹配某个技能的描述，先读取该技能文件再执行
2. 如果不匹配任何技能，直接用你自己的能力回答
3. 返回 JSON 格式：{{"matched_skill": "skill_id 或 null", "reason": "匹配原因"}}
"""


# ============ 第三部分：模拟大模型的推理过程 ============

# 用户说："帮我做个季度汇报的PPT"
# 大模型内部推理（这不是代码逻辑，是模型的语义理解）：
#   "季度汇报的PPT" → 关键词"PPT" → 语义匹配 "slides" 技能
#   → 返回：{"matched_skill": "slides", "reason": "用户需要创建PPT"}

# 用户说："这段代码为什么报错了"
# 大模型内部推理：
#   "代码报错" → 语义匹配 "debug_python" 技能
#   → 返回：{"matched_skill": "debug_python", "reason": "用户需要调试代码"}

# 用户说："今天天气怎么样"
# 大模型内部推理：
#   "天气" → 不匹配任何技能
#   → 返回：{"matched_skill": null, "reason": "不属于任何技能范畴"}
```

> **注意**：上面的 `SKILL_REGISTRY` 是为了解释原理的伪代码，实际使用中你不需要写这个。框架会自动扫描目录并生成注册表。

---

## 九、如何创建和使用 Skills？

### 9.1 你需要做什么 vs 系统自动做什么

| 谁做什么 | 手动 or 自动 |
|---|---|
| 写 SKILL.md 的内容（触发条件、执行步骤） | **手动** — 这是核心价值所在 |
| 把 SKILL.md 放到指定目录 | **手动** — 但就是放个文件 |
| 扫描目录、提取描述、构建注册表 | **自动** — 框架负责 |
| 运行时根据用户意图匹配 Skill | **自动** — 大模型负责 |
| 读取匹配到的 SKILL.md 并执行 | **自动** — 大模型负责 |

**你真正需要写的，就是 SKILL.md 这一个文件。**

### 9.2 Skill 的两种来源

**1. 自己写的 Skills（手动）**

- 创建一个 `SKILL.md` 文件
- 写清楚"这个 Skill 是干什么的"和"怎么执行"
- 放到约定的目录下（如 `~/.cursor/skills-cursor/` 或 `~/.codex/skills/`）

**2. 插件/社区提供的 Skills（半自动）**

安装插件时自动带入，例如 Figma 插件会自带一系列设计相关的 Skills。

### 9.3 一个 SKILL.md 的示例

```markdown
# FastAPI 接口构建技能

## 触发条件
当用户需要创建 FastAPI 接口、设计 API 路由、处理请求响应时使用本技能。

## 执行步骤
1. 确认接口功能和请求/响应格式
2. 生成代码时必须包含 request_id（UUID）
3. HTTP 请求库使用 aiohttp
4. 日志使用 loguru
5. 响应格式必须遵循标准格式：code / message / request_id / data

## 注意事项
- 单个文件不超过 300 行
- 注释使用 Google 风格
```

---

## 十、未来趋势

Skills 创建方式的演进路线：

```
手动写 Skill（现在主流）
  ↓
AI 观察你的工作模式，提炼出 Skill 草稿，你审核修改（正在探索）
  ↓
AI 自动观察、自动提炼、自动注册（未来愿景）
```

"你会被蒸馏成 Skills"的终极含义：未来可能不是你主动去写 SKILL.md，而是 AI 观察你日常的工作方式，自动把你的习惯和经验提炼成 Skill 文件。

---

> 本文整理于 2026-05-06，基于 Cursor IDE 中对 AI Agent Skills 的讨论。
